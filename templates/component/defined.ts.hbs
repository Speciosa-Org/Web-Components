import { {{~ jsClassName ~}} } from './index.js';

window.customElements.define(
  '{{ tagName }}',
  {{ jsClassName }},
);

declare global {
  interface HTMLElementTagNameMap {
    '{{ tagName }}': {{ jsClassName }};
  }
}
